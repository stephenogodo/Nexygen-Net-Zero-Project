# Copy to .env for local (non-Docker) runs. In Docker Compose these are set
# via docker-compose.yml / a project-root .env instead -- see .env.example.

MODEL_DIR=./models
DATA_PATH=./data/ESG_Data.csv
LOG_LEVEL=INFO
BASELINE_YEAR=2020
LAST_TRAINING_DATE=2024-10-01
MAX_FORECAST_STEPS=120
API_KEY=
